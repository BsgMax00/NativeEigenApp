import 'package:flutter/material.dart';

class GameTrailerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [
          Expanded(child:
          Center(child: Text("dummyText3"))
          )
        ],
      ),
    );
  }
}